# Hidden Search Widget

A simple hidden search widget built with vanilla JS and CSS that can be added to website.

THe search icon when clicked fires off a JS event that activates a specific class where there is a transition to make the search wider and when clicked again shrinks it back.

Netlify: https://hidden-search-widget-bw.netlify.app/
